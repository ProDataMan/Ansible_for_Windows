webservers:
  hosts:
    webserver1:
      ansible_host: 54.176.168.85
    webserver2:
      ansible_host: 13.52.179.175
  vars_files:
    - vault/credentials.yml
  vars:
    user: "{{ vault_user }}"
    pwd: "{{ vault_pwd }}"
