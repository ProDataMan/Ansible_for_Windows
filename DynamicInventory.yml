---
plugin: aws_ec2
regions:
  - us-west-1
filters:
  instance-state-name: running
keyed_groups:
  - key: tags['Role']
    prefix: tag_role
compose:
  ansible_host: private_ip_address # or public_ip_address
